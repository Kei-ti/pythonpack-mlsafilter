# pythonpack-mlsafilter

Python wrapping of MLSA filter.

## How to intall
`python setup.py install`

On MacOS, CFLAGS='-stdlib=libc++' may be needed before python to avoid a fatal error: 'ios' file not found.

## How to use
See `HOW_TO_USE.html`
